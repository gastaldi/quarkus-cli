package io.quarkus.cli.commands.extension;

import picocli.CommandLine;

@CommandLine.Command(name = "remove", description = "Remove an extension from the build descriptor")
public class ExtensionRemoveCommand implements Runnable {
    @Override
    public void run() {

    }
}
